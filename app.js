
document.addEventListener("DOMContentLoaded", () => {
    const formSection = document.getElementById("form-section");
    formSection.innerHTML = "<p>Smart RAMS dynamic interface will render here based on hazard database.</p>";
});
